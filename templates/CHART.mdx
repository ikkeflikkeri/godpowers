# Chart: [Short name]

> The chart is an INDEX, not a store. Each decision lives in exactly one
> place: its unit. The chart gists it and links. Never restate a decision
> here.
>
> Every sentence below MUST be labeled `[DECISION]`, `[HYPOTHESIS]`, or
> `[OPEN QUESTION]`. Every claim MUST fail the substitution test (swap a
> competitor name in, sentence breaks).

Slug: [kebab-slug]
Date started: [ISO 8601]
Status: charting | working | complete
Units: [N open] / [N resolved] / [N closed]

## Destination

[DECISION] [What reaching the end of this chart looks like. One or two lines.
An end state, not a restatement of the idea. The destination fixes the scope,
so every unit is judged against it.]

Bad: "Figure out the notifications system."
Good: "A written spec for notification delivery that names the transport, the
retry policy, and the per-user opt-out model, ready to hand to /god-prd."

## Notes

[DECISION] Domain: [what this effort is about].
[DECISION] Every session working this chart consults: [skills, references,
existing artifacts].
[DECISION] Standing preferences for this effort: [constraints the user has
already settled].

## Decisions so far

<!-- The index. One line per resolved unit: enough to judge relevance, then
     open the link for the detail the unit holds. Closed (out-of-scope) units
     never appear here; a scope boundary is not a step on the route. -->

- [[STORY-<slug>-001 <title>](../../stories/<slug>/STORY-<slug>-001.mdx)] - [DECISION] [one-line gist of the answer]

## Not Yet Specified

<!-- Fog of war: in-scope questions you can tell are coming but cannot yet
     phrase sharply. The test is whether you can STATE the question precisely
     now, not whether you can answer it. Do not pre-slice fog into unit-sized
     pieces; one patch may graduate into several units, or none.

     Excludes: what is already decided, what is already a live unit, and what
     is out of scope. -->

- [OPEN QUESTION] [The dim shape of a question. As loose or as full as the
  view allows.]

## Out of Scope

<!-- Work consciously ruled beyond the destination. Scope, not sharpness,
     lands it here. This never graduates: if the destination is redrawn, that
     is a fresh chart, not a resumption. -->

- [DECISION] [Gist] - ruled out because [reason]. ([STORY-<slug>-00N](../../stories/<slug>/STORY-<slug>-00N.mdx))

## Handoff

[DECISION] When no units remain open and no fog remains, this chart is done.
Route to: [/god-prd | /god-feature | /god-build] with the Decisions so far as
input.

---

## Have-Nots Checklist

- [ ] Destination names an end state, not a restatement of the idea (W-01)
- [ ] No decision is restated here; each lives in its unit (W-02)
- [ ] No unit ships behavior instead of resolving a question (W-03)
- [ ] Fog is written as patches, not pre-sliced into units (W-04)
- [ ] Out-of-scope work is in Out of Scope, not Not Yet Specified (W-05)
- [ ] Closed units carry a reason and stay out of Decisions so far (W-08)
- [ ] Every graduated fog patch was deleted from Not Yet Specified
