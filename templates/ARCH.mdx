# System Architecture

> Every decision below has a flip point. Every claim is substitution-tested.
> Every NFR from PRD has a corresponding architectural choice.

## Options Considered

The system shapes evaluated before the system context and containers below were
chosen. If exactly one shape was considered, say so here and give the reason;
do not leave the section empty.

| Shape | Score | Why rejected | Trap flag |
|-------|-------|--------------|-----------|
| [Baseline: the obvious shape] | [N.N/10] | [Chosen, or the specific reason it lost] | [HYPOTHESIS] tradeoff: [one line naming what it gives up] |
| [Alternative shape] | [N.N/10] | [Specific reason, tied to a PRD NFR or ADR] | |
| [Alternative shape] | [N.N/10] | [Specific reason] | [HYPOTHESIS] hidden cost: [one line] |

Score against the NFR map and flip points below, including any trap demotion.
Novelty is not a scoring axis.

## System Context (C4 Level 1)

```
                    [External User]
                          |
                          v
                   ┌──────────────┐
[External API] -> │  THE SYSTEM  │ -> [External Service]
                   └──────────────┘
                          |
                          v
                   [External DB / Service]
```

[Each arrow labeled with: data flowing, protocol, frequency.]

## Container Diagram (C4 Level 2)

```
[Container 1] --[API/protocol]--> [Container 2]
     |
     v
[Container 3 (DB)]
```

| Container | Single Responsibility | Technology |
|-----------|----------------------|------------|
| [Name] | [One sentence. Single thing it owns.] | [Language/framework] |

## Critical Flows

An ordered interaction for every flow that crosses a trust boundary or touches
more than two containers. Number each step; mark the steps that write state,
because those are the steps a retry must not repeat.

```
[Flow name] (crosses [boundary], touches [N] containers)

1. [Caller]  -> [Callee]   [operation]
2. [Caller]  -> [Callee]   WRITE [what]
3. [Caller]  -> [Callee]   [operation]
```

| Flow | Crosses | Writes at | Fails visibly at |
|------|---------|-----------|------------------|
| [Name] | [Boundary] | [step numbers] | [step, or "nowhere"] |

A flow that fails visibly nowhere needs a row in Failure and Degradation below.

## Architecture Decision Records

Only create an ADR when all three are true: the decision is hard to reverse,
surprising without context, and the result of a real tradeoff.

### ADR-001: [Decision Title]
- **Context**: [What forced this decision]
- **Decision**: [What was chosen]
- **Rationale**: [Name each alternative considered and why this beat it]
- **Flip point**: [Conditions under which this decision reverses]
- **Consequences**: [What this makes easier; what it makes harder]

### ADR-002: [Decision Title]
[Same structure]

## NFR-to-Architecture Map

| PRD NFR | Architectural Choice | ADR Reference |
|---------|---------------------|---------------|
| p99 < 100ms | [Choice that delivers this] | ADR-00X |
| 99.9% uptime | [Choice that delivers this] | ADR-00X |
| [Other NFR] | [Choice] | ADR-00X |

Every NFR from PRD MUST appear here. If an NFR has no corresponding choice,
flag it.

## Capacity Envelope

The NFR map states the targets. This states the load they are measured under,
and where each number came from.

| Input | Today | At the PRD horizon | Source |
|-------|-------|--------------------|--------|
| [What drives load] | [N] | [N] | [PRD metric / measurement / [HYPOTHESIS] + validation plan] |

Derived, per container:

| Container | Derived load | Headroom on the chosen shape |
|-----------|--------------|------------------------------|
| [Name] | [arithmetic from the inputs above] | [how much room is left] |

**Saturates first**: [container], at [the number that triggers it]. That number,
not a round user count, is what the relevant ADR flip point should name.

Every number is an input with a source or arithmetic over inputs with sources.
An unlabeled guess is a have-not (A-15).

## Trust Boundaries

```
[External]
    |
=== TRUST BOUNDARY: [Auth method, data classification] ===
    |
[Internal]
```

For each external integration:
- **Boundary**: [Where it sits]
- **Auth model**: [How identity is established]
- **Data classification**: [What flows across; sensitive or public]
- **Failure mode**: [What happens if the boundary is breached]

## Failure and Degradation

Trust Boundaries covers the edge being breached. This covers the same edge being
slow, down, or wrong.

| Dependency | Timeout | On failure | Idempotent | User sees |
|------------|---------|------------|------------|-----------|
| [External service] | [Ns connect, Ns read] | [retry count, backoff, terminal state] | [Yes, on what key / No, why] | [the degraded state, in words] |
| [Datastore] | [Ns] | [fail closed / fail open] | [n/a] | [what the caller returns] |

**Backpressure**: [the queue depth or rate at which new work is rejected rather
than accepted]. A queue with no rejection point is an outage with a delay.

**Recovery**: [the capped rate the backlog drains at after the dependency
returns].

For each container in the container diagram, what happens if it dies:

| Container | If it dies | Verdict |
|-----------|-----------|---------|
| [Name] | [blast radius, in words] | [SPOF, addressed by X / SPOF, accepted because Y / degrades only] |

Every "everything stops" row is addressed or accepted in writing (A-17).

## Data Model

### Entities

| Entity | Owner Service | Consistency Model |
|--------|--------------|-------------------|
| User | auth-service | Strong |
| Order | orders-service | Strong (own DB), Eventual (read replicas) |

### Relationships
- [User] 1:N [Order]
- [Description of relationships]

---

## Have-Nots Checklist

Before declaring done, verify:
- [ ] Every container has a clear single responsibility
- [ ] No two containers share responsibility without justification
- [ ] Every NFR from PRD has an architectural mapping
- [ ] Every ADR has a flip point
- [ ] "Scalable" never appears without numbers
- [ ] Every external integration has a trust boundary
- [ ] Every data entity has an ownership assignment
- [ ] Every boundary-crossing flow has an ordered interaction
- [ ] Every capacity number traces to an input with a source
- [ ] Every dependency has a timeout, a retry stance, and a degraded state
- [ ] Every container whose loss stops the system is addressed or accepted
- [ ] No sentence is unlabeled
